import { config } from './app';

export const barcodeConfig = {
  type: config.barcode.type,
  scale: config.barcode.scale,
  height: config.barcode.height,
  background: config.barcode.background,
  color: config.barcode.color,
};